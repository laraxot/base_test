Configuration files, for localhost or multi-domains.

The most important files are:

xra.php (with default themes and other things)

morph_map (to assign model names)

moudle_status.json (to enable Laravel modules)